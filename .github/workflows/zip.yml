name: Zip into spoon archive
on:
  push:
    branches:
      - main

jobs:
  zip-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make folder
        run: |
          mkdir Spoons
          mkdir Highlight.spoon
          cp *.lua Highlight.spoon

      - name: Create zip
        run: zip -r -q Spoons/Highlight.spoon.zip Highlight.spoon

      - name: Commit and push
        uses: EndBug/add-and-commit@v9
        with:
          add: '["Spoons/Highlight.spoon.zip --force"]'
          message: "Bundle into zip"
          push: origin release --set-upstream --force
          new_branch: release
